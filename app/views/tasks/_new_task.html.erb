<% @task ||= Task.new %>
<div class="form_wrapper">
	<%= form_for @task do |f| %>
		<div class="field">
			<%= f.text_field :content, :class => "text" %>&nbsp;<%= f.submit("↵") %>
		</div>
	<% end %>
</div>

